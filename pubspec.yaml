name: harpy
description: A Twitter app built with Flutter.

publish_to: 'none'

# android/version.properties is used to manage the versions
version: 0.0.0

environment:
  sdk: ">=2.9.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # packages
  cached_network_image: ^3.0.0-nullsafety
  cupertino_icons: ^1.0.2
  dart_twitter_api: ^0.5.1
  downloads_path_provider: ^0.1.0 # non null safe
  equatable: ^2.0.0
  flare_flutter: ^2.0.6 # non null safe
  flutter_bloc: ^7.0.0-nullsafety.4
  flutter_colorpicker: ^0.4.0-nullsafety.0
  flutter_feather_icons: ^1.0.3 # non null safe
  flutter_material_color_picker: ^1.0.5 # non null safe
  get_it: ^6.0.0
  humanize: ^0.3.0 # non null safe
  intl: ^0.17.0
  like_button: ^1.0.4 # non null safe
  logging: ^1.0.0
  mime_type: ^0.3.2 # non null safe
  provider: ^5.0.0
  sentry: ^4.1.0-nullsafety.0
  timeago: ^3.0.1
  url_launcher: ^6.0.2
  visibility_detector: ^0.2.0-nullsafety.1

  # plugins
  connectivity: ^3.0.2
  device_info: ^2.0.0
  file_picker: ^3.0.0
  firebase_analytics: any # non null safe
  flutter_ffmpeg: ^0.3.1 # non null safe
  flutter_downloader: ^1.5.2 # non null safe
  flutter_twitter_login: # non null safe
    git: git://github.com/robertodoering/flutter_twitter_login.git
  keyboard_visibility: ^0.5.6 # non null safe
  package_info: ^2.0.0
  path_provider: ^2.0.1
  permission_handler: ^6.0.1
  share: ^2.0.0
  shared_preferences: ^2.0.3
  video_player: ^2.0.0

dev_dependencies:
  bloc_test: ^8.0.0-nullsafety.2
  build_runner: ^1.11.5
  flutter_test:
    sdk: flutter
  json_serializable: ^4.0.2
  mockito: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/flare/
    - android/fastlane/metadata/android/free/en-US/changelogs/

  fonts:
  - family: Comfortaa
    fonts:
    - asset: assets/fonts/Comfortaa-Light.ttf
      weight: 300
    - asset: assets/fonts/Comfortaa-Regular.ttf
    - asset: assets/fonts/Comfortaa-Bold.ttf
      weight: 700

  - family: OpenSans
    fonts:
    - asset: assets/fonts/OpenSans-Light.ttf
      weight: 300
    - asset: assets/fonts/OpenSans-Regular.ttf
    - asset: assets/fonts/OpenSans-Bold.ttf
      weight: 700

  - family: NotoSans
    # used as a fallback font
    # covers a lot of scripts and obscure unicode characters
    # https://fonts.google.com/specimen/Noto+Sans
    fonts:
      - asset: assets/fonts/NotoSans-Regular.ttf
      - asset: assets/fonts/NotoSans-Bold.ttf
        weight: 700
